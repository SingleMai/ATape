interface:
  display_name: "ATape Adapter 接入"
  short_description: "从真实会话源推进 Harness 接入、持续采集验证和安装验收"
  default_prompt: "使用 $atape-adapter-integration，按本次约定的 Harness 或格式支持范围完成 ATape 接入。"
policy:
  allow_implicit_invocation: false
